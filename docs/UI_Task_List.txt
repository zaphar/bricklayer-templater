Manage ->
	Content ->
		List
		Add
		Update
		Organize
	Users ->
		List
		Add
		Update
		Organize
	Notes:
		All Ajax dynamic
		
Browse ->
	By Meta Query ->
	
	By Hierarchy ->
	
	By Navigation ->

Tag handlers:

	basic handlers ->
		row (loops through an array or hash)
		item (outputs whatever it gets passed as an argument)
		hashitem (outputs the value of a key in a hash)
		arrayitem (outputs the value of an index in an array)
		bind_output (basically just binds to an action plugin and passes data on)
	Custom Handlers
		title_page
		breadcrumbs (bind_output->row->item handler instead?)

Actions:
	search
	upload
	write
	store_content
	link
	add_meta
	delete_meta
	get_meta_types
	get_meta_values
	get_content
	display_content
	
	